# 05 - 业务场景与高阶交互规范 (Business Scenarios & Advanced UX)

> **适用系统**：`{{PRODUCT_NAME}}` (`{{PRODUCT_CN_NAME}}`)  
> **核心场景**：多角色协同、权限提级审批流、防御性表单、全局无障碍与快捷键

---

## 🎭 一、多角色动态切换机制 (Role Switching Widget)

为便于在单页面演示或真实多租户权限系统下验证不同角色的界面表现，系统内置右下角浮动角色模拟切换器：

### 1. 角色定义与视觉标识
- **{{ROLE_USER}}**：标识色蓝灰，仅具备资产检索、查看文档与普通调用权限。
- **{{ROLE_ADMIN}}**：标识色深蓝，具备资产配置、团队成员管理、普通发布权限。
- **{{ROLE_AUDITOR}}**：标识色紫色/橙色，具备安全门禁放行、权限提级终审、合规封禁强制权。

### 2. 界面自适应规则
```javascript
// 角色切换事件监听与 UI 广播
function setDemoRole(role) {
  document.documentElement.setAttribute('data-user-role', role);
  localStorage.setItem('demo_role', role);
  
  // 动态控制高权限 DOM 显隐
  const adminElements = document.querySelectorAll('[data-role-required="admin"]');
  const auditorElements = document.querySelectorAll('[data-role-required="auditor"]');
  
  adminElements.forEach(el => {
    el.style.display = (role === 'admin' || role === 'auditor') ? '' : 'none';
  });
  auditorElements.forEach(el => {
    el.style.display = (role === 'auditor') ? '' : 'none';
  });
}
```

---

## 🛡️ 二、资产可见性与风险提级流程 (Visibility Promotion Flow)

当资产需要从「个人私有」或「团队内部」晋升为「全企业公开」或「核心系统直通」时，必须触发标准提级流水线：

```
                    ┌─────────────────────────┐
                    │ 1. 发起提级申请         │ (开发者填写提级理由与目标等级)
                    └────────────┬────────────┘
                                 │
                                 ▼
                    ┌─────────────────────────┐
                    │ 2. 自动化三引擎安全扫描  │ (SAST 静态代码 + 敏感密钥 + 许可证合规)
                    └────────────┬────────────┘
                                 │
                 ┌───────────────┴───────────────┐
                 │ 扫描未通过                    │ 扫描全部 PASS
                 ▼                               ▼
    ┌─────────────────────────┐     ┌─────────────────────────┐
    │ 阻断并生成修复建议报告   │     │ 3. 触发人工终审审批流   │ (由 {{ROLE_AUDITOR}} 审查)
    └─────────────────────────┘     └────────────┬────────────┘
                                                 │
                                                 ▼
                                    ┌─────────────────────────┐
                                    │ 4. 签发提级凭证与审计存证│ (写入不可篡改操作流水)
                                    └─────────────────────────┘
```

---

## ⌨️ 三、键盘快捷键与无障碍体系 (Shortcuts & Accessibility)

为了给专业开发者与管理员提供如同 Desktop 级别的极致操作体验：

| 快捷键 (Mac / Win) | 功能描述 | 响应动作 |
| :--- | :--- | :--- |
| `Cmd + K` / `Ctrl + K` | 全局命令面板与快速检索 | 弹出聚焦全局 Command Palette 搜索框 |
| `Cmd + B` / `Ctrl + B` | 侧边栏/子导航折叠 | 扩大内容可视阅读区域 |
| `G then H` | 快捷直达首页 (Go Home) | 路由跳转至资产市场首页 |
| `G then S` | 快捷直达安全审计中心 | 路由跳转至 Security Center |
| `Esc` | 退出所有模态 | 关闭当前激活的抽屉、弹窗或搜索浮层 |

---

## 🕳️ 四、异常、空态与加载态规范 (Empty, Skeleton & Error States)

### 1. 骨架屏加载 (Skeleton Loading)
- 数据加载时禁止使用突兀的全屏 Spinner，必须采用微光脉冲骨架屏（Shimmer Skeleton）。
```css
@keyframes skeleton-shimmer {
  0% { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}
.skeleton {
  background: linear-gradient(90deg, var(--bg-subtle) 25%, var(--border) 50%, var(--bg-subtle) 75%);
  background-size: 200% 100%;
  animation: skeleton-shimmer 1.5s infinite;
  border-radius: var(--r-sm);
}
```

### 2. 标准空状态规范 (Empty State)
- 包含三要素：**极简线条插画/图标** + **清晰的无结果原因** + **明确的下一步行动引导（CTA）**。
```html
<div class="empty-state">
  <div class="empty-icon">🔍</div>
  <h3 class="empty-title">未检索到匹配的{{CORE_ASSET_NAME}}</h3>
  <p class="empty-desc">请尝试调整搜索关键词、清除风险层级过滤项，或自主创建新资产。</p>
  <div class="empty-actions">
    <button class="btn btn-secondary">重置筛选条件</button>
    <button class="btn btn-primary">发布新{{CORE_ASSET_NAME}}</button>
  </div>
</div>
```
