# 04 - 布局骨架与页面模板规范 (Layout & Page Templates)

> **适用系统**：`{{PRODUCT_NAME}}` (`{{PRODUCT_CN_NAME}}`)  
> **核心架构**：双层复合导航（Primary Header + Subnav Context）+ 响应式栅格系统

---

## 📐 一、全局布局骨架结构 (Global Layout Skeleton)

整个系统的基础页面外框由 **顶层品牌导航 (TopNav)**、**业务上下文二级栏 (SubNav)** 与 **主内容区域 (Main Content)** 组成：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 品牌 Logo  │  搜索框 (Cmd+K)       │ 快捷链接   │ 通知 🔔 │ 主题切换 🌓 │ 用户头像 │  <- TopNav (56px)
├─────────────────────────────────────────────────────────────────────────────┤
│ 📦 探索市场   🛡️ 审计中心   📊 监控指标   ⚙️ 组织配置         [ 快速发布 + ]   │  <- SubNav (44px)
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  主内容容器 (Main Content: max-width 1280px / 1440px, centered)              │
│                                                                             │
│  ┌───────────────────────┐ ┌───────────────────────┐ ┌───────────────────┐  │
│  │      Asset Card       │ │      Asset Card       │ │    Asset Card     │  │
│  └───────────────────────┘ └───────────────────────┘ └───────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 响应式断点系统 (Breakpoints)
- **Mobile (`< 640px`)**：隐藏二级导航，侧滑抽屉折叠，卡片单列（1 Col）。
- **Tablet (`640px ~ 1024px`)**：卡片双列（2 Col），表格支持水平滚动。
- **Desktop (`1024px ~ 1440px`)**：标准主容器（1280px），卡片三列（3 Col）。
- **Wide (`> 1440px`)**：宽屏容器（1440px），卡片四列（4 Col）。

---

## 📄 二、五大经典业务页面骨架模板

### 模板 1: 资产市场探索页 (Asset Marketplace / Home)
* **核心目标**：高效检索、分类筛选、多视图浏览（Grid vs Table）
* **结构区块**：
  1. **Hero 搜索与统计概览**：大输入框、快捷热门标签（如 `#认证`、`#网关`、`#数据提取`）、资产总数与健康率。
  2. **过滤控制栏 (Filter Toolbar)**：
     - 分类标签切换（全部 / 官方 / 部门自建 / 社区）。
     - 风险层级筛选（L0 ~ L3 下拉菜单）。
     - 排序方式（最多安装、最新发布、评分最高）。
     - 视图切换器（网格卡片图标 / 列表表格图标）。
  3. **资产主体展示区**：响应式卡片网格或分页数据表。

---

### 模板 2: 资产详情与接入页 (Asset Detail & Integration)
* **核心目标**：提供完整的资产元数据、接入代码示例、版本历史与权限申请
* **结构区块**：
  1. **资产 Header**：Logo、`{{ASSET_NAMESPACE_PREFIX}}/name`、维护者头像、风险等级徽章、主要行动按钮（`一键集成`、`申请提级`）。
  2. **左右双栏分流**：
     - **左侧主内容区 (65%)**：
       - `Markdown 文档渲染区`：功能介绍、输入输出 Schema 说明。
       - `交互式接入代码块 (Code Sandbox)`：提供 cURL / Python / TypeScript 一键复制。
       - `变更日志 (Changelog)` 与版本历史时间线。
     - **右侧元数据侧边栏 (35%)**：
       - 三引擎安全合规扫描状态报告。
       - 权限范围与依赖项清单。
       - 维护团队与支持 SLA 级别。

---

### 模板 3: 资产发布与提级向导 (Publish & Promotion Wizard)
* **核心目标**：标准化发布引导、防御性合规检查、分步骤提级
* **步骤条设计 (Stepper)**：
  - **Step 1: 基础信息填报**（名称、命名空间、版本、简介、分类标签）。
  - **Step 2: 资产包与依赖上传**（代码包、配置 YAML、依赖声明）。
  - **Step 3: 自动化安全门禁扫描**（SAST 代码检查、密钥扫描、合规检查）。
  - **Step 4: 权限与可见性声明**（选择公开范围、指定责任人）。
  - **Step 5: 终审提交与流水线状态**。

---

### 模板 4: 企业安全与审计治理中心 (Security & Audit Center)
* **核心目标**：实时监控高危资产、权限提级审批、审计日志存证
* **结构区块**：
  1. **风险态势看板 (KPI Stat Grid)**：
     - 当前全网资产总数
     - 待审核提级请求（Pending Reviews）
     - 存在合规告警资产数（L2/L3 Risks）
  2. **待办审批流工作台 (Approval Inbox)**：
     - 支持批量驳回、批准提级、转交审查员。
  3. **不可篡改审计日志流水 (Audit Trail Table)**：
     - 时间戳（Mono 字体）、操作人、操作类型（`UPGRADE_VISIBILITY`）、目标资产、客户端 IP 与哈希存证。

---

### 模板 5: 设计规范活体参考页 (Living Style Guide - `design-system.html`)
* **核心目标**：作为全局设计资产的单一真实数据源（Single Source of Truth），统一团队标准。
* **结构区块**：
  - 侧边栏快速锚点导航（色彩色板、字体样本、按钮体系、卡片体系、表格、模态与抽屉等）。
  - 实时主题（浅色/深色）切换实时渲染。
  - 组件各种边界状态展示（Normal、Hover、Active、Disabled、Empty、Loading）。
