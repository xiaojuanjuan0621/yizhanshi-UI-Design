# 03 - 组件库规范与设计模式 (Component Catalog & Patterns)

> **适用系统**：`{{PRODUCT_NAME}}` (`{{PRODUCT_CN_NAME}}`)  
> **分类体系**：Atomic Design 理念（原子 Atoms ➔ 分子 Molecules ➔ 复合组织 Organisms ➔ 模态与抽屉 Drawers/Modals）

---

## 🔬 一、基础原子组件 (Atoms)

### 1. 风险与状态徽章 (RiskBadge & StatusBadge)
用于标明资产风险分级、审批状态、健康度。

* **DOM 结构**：
```html
<!-- 风险徽章 -->
<span class="badge badge-risk-l0"><span class="badge-dot"></span>{{STATUS_L0_NAME}}</span>
<span class="badge badge-risk-l1"><span class="badge-dot"></span>{{STATUS_L1_NAME}}</span>
<span class="badge badge-risk-l2"><span class="badge-dot"></span>{{STATUS_L2_NAME}}</span>
<span class="badge badge-risk-l3"><span class="badge-dot"></span>{{STATUS_L3_NAME}}</span>

<!-- 基础语义标签 -->
<span class="badge badge-outline">官方认证</span>
<span class="badge badge-brand">企业自建</span>
```
* **CSS 样式规范**：
```css
.badge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 2px 8px;
  font-size: 12px;
  font-weight: 500;
  border-radius: var(--r-pill);
  line-height: 16px;
  white-space: nowrap;
}
.badge-dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
}
.badge-risk-l0 { background: #ECFDF5; color: #059669; border: 1px solid #A7F3D0; }
.badge-risk-l0 .badge-dot { background: #10B981; }

.badge-risk-l1 { background: #EFF6FF; color: #2563EB; border: 1px solid #BFDBFE; }
.badge-risk-l1 .badge-dot { background: #3B82F6; }

.badge-risk-l2 { background: #FFFBEB; color: #D97706; border: 1px solid #FDE68A; }
.badge-risk-l2 .badge-dot { background: #F59E0B; }

.badge-risk-l3 { background: #FEF2F2; color: #DC2626; border: 1px solid #FECACA; }
.badge-risk-l3 .badge-dot { background: #EF4444; }
```

---

### 2. 指标数据胶囊 (StatChip / MetricPill)
用于在卡片或详情页快速呈现核心指标（如安装量、评分、版本号、延迟）。

* **HTML 示例**：
```html
<div class="stat-chip">
  <svg class="icon" ...></svg>
  <span class="stat-label">{{METRIC_1}}</span>
  <span class="stat-value font-mono">14.2k</span>
</div>
```
* **CSS 规范**：
```css
.stat-chip {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 2px 6px;
  background: var(--bg-subtle);
  color: var(--text-secondary);
  border-radius: var(--r-sm);
  font-size: 12px;
}
.stat-value {
  color: var(--text-primary);
  font-weight: 600;
  font-family: var(--font-mono);
}
```

---

### 3. 操作按钮组 (Buttons)
支持 4 档语义层级：`Primary（主操作）`、`Secondary（次操作）`、`Outline（线框）`、`Danger（危险/阻断）`。

```css
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  height: 36px;
  padding: 0 14px;
  font-size: 14px;
  font-weight: 500;
  border-radius: var(--r-sm);
  cursor: pointer;
  transition: all var(--ease-fast);
  border: 1px solid transparent;
}
.btn-sm { height: 28px; padding: 0 10px; font-size: 12px; }
.btn-lg { height: 42px; padding: 0 20px; font-size: 16px; }

.btn-primary {
  background: var(--brand);
  color: #FFFFFF;
}
.btn-primary:hover { background: var(--brand-hover); }

.btn-secondary {
  background: var(--bg-subtle);
  color: var(--text-primary);
  border-color: var(--border);
}
.btn-secondary:hover { background: var(--border); }

.btn-accent {
  background: var(--accent);
  color: #FFFFFF;
}
.btn-accent:hover { opacity: 0.9; }

.btn-danger {
  background: #FEF2F2;
  color: #DC2626;
  border-color: #FECACA;
}
.btn-danger:hover { background: #DC2626; color: #FFFFFF; }
```

---

## 📦 二、复合业务组件 (Molecules & Organisms)

### 1. 核心资产卡片 (AssetCard / SkillCard) - 双视图支持

#### (1) 网格卡片形态 (Grid View Card)
* **结构布局**：
```html
<div class="asset-card">
  <div class="card-header">
    <div class="asset-avatar">📦</div>
    <div class="asset-meta">
      <div class="asset-namespace font-mono">{{ASSET_NAMESPACE_PREFIX}}/auth-guard</div>
      <h3 class="asset-title">企业级认证拦截守卫</h3>
    </div>
    <span class="badge badge-risk-l0">{{STATUS_L0_NAME}}</span>
  </div>
  
  <p class="asset-desc">
    提供多租户 JWT 验签、细粒度 RBAC 校验与网关层动态速率限制，保障下游服务安全。
  </p>
  
  <div class="card-footer">
    <div class="stat-group">
      <span class="stat-chip"><span class="stat-value">v1.4.2</span></span>
      <span class="stat-chip"><span class="stat-value">28.9k</span> {{METRIC_1}}</span>
    </div>
    <button class="btn btn-sm btn-secondary">配置接入</button>
  </div>
</div>
```

* **卡片微交互与 1px 边框样式**：
```css
.asset-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--r-md);
  padding: var(--space-4);
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
  transition: all var(--ease-normal);
  box-shadow: var(--shadow-xs);
}
.asset-card:hover {
  border-color: var(--border-hover);
  transform: translateY(-2px);
  box-shadow: var(--shadow-md);
}
.asset-namespace {
  font-size: 12px;
  color: var(--accent);
}
.asset-title {
  font-size: 16px;
  font-weight: 600;
  color: var(--text-primary);
  margin: 0;
}
.asset-desc {
  font-size: 13px;
  color: var(--text-secondary);
  line-height: 1.5;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  margin: 0;
}
.card-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: auto;
  padding-top: var(--space-3);
  border-top: 1px solid var(--border);
}
```

---

### 2. 企业权限对比矩阵表 (PermissionMatrixTable)
用于清晰展示普通用户、管理员与审查员在不同资源维度的操作权限（基于 GitLab Pajamas / AWS IAM 风格）。

```html
<table class="data-table permission-matrix">
  <thead>
    <tr>
      <th>资源操作类型</th>
      <th>{{ROLE_USER}}</th>
      <th>{{ROLE_ADMIN}}</th>
      <th>{{ROLE_AUDITOR}}</th>
      <th>风险门禁要求</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td class="font-medium">检索与在线调用 (Read & Invoke)</td>
      <td><span class="text-success">✔ 允许</span></td>
      <td><span class="text-success">✔ 允许</span></td>
      <td><span class="text-success">✔ 允许</span></td>
      <td><span class="badge badge-risk-l0">无门禁</span></td>
    </tr>
    <tr>
      <td class="font-medium">资产提级与发布 (Visibility Upgrade)</td>
      <td><span class="text-muted">✖ 禁止</span></td>
      <td><span class="text-warning">⚠ 需二次确认</span></td>
      <td><span class="text-success">✔ 终审放行</span></td>
      <td><span class="badge badge-risk-l2">双人复核</span></td>
    </tr>
    <tr>
      <td class="font-medium">下线与合规封禁 (Revoke & Deprecate)</td>
      <td><span class="text-muted">✖ 禁止</span></td>
      <td><span class="text-danger">✖ 需审计审批</span></td>
      <td><span class="text-success">✔ 拥有强制权</span></td>
      <td><span class="badge badge-risk-l3">审计日志存证</span></td>
    </tr>
  </tbody>
</table>
```

---

### 3. 三引擎安全合规扫描卡片 (SecurityScanCard)
用于资产发布或审计流程中展示自动化检测结果。

```html
<div class="security-scan-card">
  <div class="scan-engine-item pass">
    <div class="engine-icon">🛡️</div>
    <div class="engine-info">
      <div class="engine-title">代码静态分析 (SAST Engine)</div>
      <div class="engine-desc">未检出高危命令执行、SQL注入与反序列化漏洞</div>
    </div>
    <span class="badge badge-risk-l0">PASS (0/0)</span>
  </div>

  <div class="scan-engine-item pass">
    <div class="engine-icon">🔍</div>
    <div class="engine-info">
      <div class="engine-title">敏感凭据防泄漏 (Secret Detector)</div>
      <div class="engine-desc">已脱敏处理，未包含硬编码 API Key / 私钥证书</div>
    </div>
    <span class="badge badge-risk-l0">PASS</span>
  </div>

  <div class="scan-engine-item warn">
    <div class="engine-icon">⚖️</div>
    <div class="engine-info">
      <div class="engine-title">开源合规与许可证 (License Compliance)</div>
      <div class="engine-desc">包含 1 项 GPL-3.0 传染性开源依赖项，需合规确认</div>
    </div>
    <span class="badge badge-risk-l2">REVIEW NEEDED</span>
  </div>
</div>
```

---

## 🪟 三、高级交互容器 (Drawers & Modals)

### 1. 右侧角色/权限配置抽屉 (RoleAssignmentDrawer, 400px)
* **交互规范**：从屏幕右侧滑出，带半透明遮罩，点击遮罩或 `Esc` 键退出，头部固定标题与关闭按钮，底部固定操作栏（取消/确认保存）。
* **CSS 框架**：
```css
.drawer-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(15, 23, 42, 0.5);
  backdrop-filter: blur(2px);
  z-index: 1000;
  opacity: 0;
  pointer-events: none;
  transition: opacity var(--ease-normal);
}
.drawer-backdrop.active {
  opacity: 1;
  pointer-events: auto;
}
.drawer-panel {
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  width: 420px;
  max-width: 90vw;
  background: var(--bg-card);
  border-left: 1px solid var(--border);
  box-shadow: var(--shadow-lg);
  display: flex;
  flex-direction: column;
  transform: translateX(100%);
  transition: transform var(--ease-normal);
  z-index: 1001;
}
.drawer-backdrop.active .drawer-panel {
  transform: translateX(0);
}
.drawer-header {
  padding: var(--space-4) var(--space-6);
  border-bottom: 1px solid var(--border);
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.drawer-body {
  flex: 1;
  padding: var(--space-6);
  overflow-y: auto;
}
.drawer-footer {
  padding: var(--space-4) var(--space-6);
  border-top: 1px solid var(--border);
  display: flex;
  justify-content: flex-end;
  gap: var(--space-3);
  background: var(--bg-subtle);
}
```
